<header>
  <h3><%= t("user") + t("blank") + t("mgmt") %></h3>
  <ul class="actions">
    <li>
      <%= link_to t("create") + t("blank") + t("user"),
        new_admin_user_path, class: "new" %>
    </li>
  </ul>
</header>
<ul>
  <% @users.each do |user| %>
  <li>
    <%= link_to user, [:admin, user] %>
  </li>
  <% end %>
</ul>
