module DocsHelper
  def state_transition_for(comment)
    #   if comment.previous_state != comment.state
    #     content_tag(:p) do
    #       value = "<strong><i class='fa fa-gear'></i> 状态改变</strong>"
    #       if comment.previous_state.present?
    #         value += " 从 #{render comment.previous_state}"
    #       end
    #       value += " 为 #{render comment.state}"
    #       value.html_safe
    #     end
    #   end
  end
end
